version: '3.8'

services:
  liteflow-demo:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    volumes:
      - ./src:/app/src
      - ./pom.xml:/app/pom.xml
    command: ./mvnw spring-boot:run